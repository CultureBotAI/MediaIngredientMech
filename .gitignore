# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.venv/
venv/
ENV/
env/
.env
.env.*
!.env.example

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# LinkML generated
src/mediaingredientmech/datamodel/

# Data snapshots (keep curated data in git)
data/snapshots/

# Ad-hoc curated-data backups. Git history and `just snapshot` are the supported
# recovery mechanisms; do not commit editor/script backup suffixes.
data/curated/*.bak
data/curated/*.backup*
data/curated/*.pre-merge-*
data/curated/*.pre-dedup-*

# Logs and reports
*.log
output/
reports/
research/

# Temporary files
tmp/
temp/
*.tmp
.umap_cache/
data/embeddings/*.tsv.gz
*.tsv.gz

# Generated per-ingredient HTML (regenerated by `just gen-ingredient-pages`).
pages/ingredient/
pages/index.html
pages/style.css

# Local Claude Code runtime state.
.claude/scheduled_tasks.lock

# Pipeline output / cache / analysis artifacts (regenerable from current YAMLs).
data/curated/backups/
references_cache/
data/analysis/
enrichment_failures.json

# linkml-term-validator / OAK label cache
cache/

# Per-record export backups (regenerable; mirrors data/curated/backups/).
data/ingredients/*/backups/

# Full machine-readable findings dump from validate_records_against_research.py.
# Regenerates on every run and grows with the sweep (11k+ lines at 527/2841
# records). The curator-facing .tsv and .md next to it stay tracked.
mappings/record_research_validation.json
