# `data/ingredients/mapped/LL-37.yaml`

## Verdict

Pass. The CultureBotHT exact CHEBI:81664 identity, empty synonym payload, empty
final `other` field, and final SSSOM row are consistent.

## Identity

- Reviewed record: `data/ingredients/mapped/LL-37.yaml`.
- Identifier and grounding: `identifier: CHEBI:81664` with
  `ontology_mapping.ontology_id: CHEBI:81664`, label `LL-37`, source `CHEBI`,
  `mapping_quality: EXACT_MATCH`, `mapping_status: MAPPED`, and
  `ingredient_type: SINGLE_INGREDIENT`.

## Validation

- `uv run --frozen python scripts/validate_strict.py` on the 5-file batch from
  `L-valine` through `L_-tartaric_Acid`: exited 0 and wrote zero ERROR rows.
- `uv run --frozen linkml-term-validator validate-data data/ingredients/mapped/L-valine.yaml data/ingredients/mapped/LL-37.yaml data/ingredients/mapped/L_-tartaric_Acid.yaml -s src/mediaingredientmech/schema/mediaingredientmech.yaml -t IngredientRecord --labels`:
  exited 0 for the three CHEBI-primary records.

## Evidence

- EBI OLS4 resolves `CHEBI:81664` as active `LL-37` and lists KEGG compound
  cross-reference `C18313`.
- The imported CultureBotHT row exact-matched the active ChEBI label and had no
  CAS value to preserve.
- The final SSSOM publishes one `skos:exactMatch` row to `CHEBI:81664` with an
  empty `other` field, so no unreviewed synonyms are exported.
- The hidden and ignored-inclusive search over `data`, `mappings`, `reports`,
  `docs`, `src`, and `tests` found the current YAML, aggregate copy, final
  SSSOM row, docs projections, and an unrelated unmapped LL-37 fragment record.

## Completeness

- The active ChEBI identity, empty synonym list, aggregate copy, and final SSSOM
  row are present and consistent.
- The empty CultureMech occurrence count is expected for this CultureBotHT CAS
  table import because no recipe occurrences were attached.

## Recommended Edits

- None.
