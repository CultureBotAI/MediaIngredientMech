# `data/ingredients/mapped/Cecl3_X_7_H2o.yaml`

## Verdict

Needs curation; major issue. The seven-water cerium trichloride identity is now
kept on its own local identifier and close-matched to anhydrous `CHEBI:35458`,
but anhydrous structure fields remain under `chemical_properties`, the hydrate
formula is only an inferred dot-water repair, and the TRACE_ELEMENT role is
still provisional.

## Identity

- Reviewed record: `data/ingredients/mapped/Cecl3_X_7_H2o.yaml`.
- Identifier and grounding:
  `identifier: kgmicrobe.compound:cecl3_x_7_h2o`,
  `ontology_mapping.ontology_id: CHEBI:35458`,
  `ontology_label: cerium trichloride`, `ontology_source: CHEBI`,
  `mapping_quality: CLOSE_MATCH`, `mapping_status: MAPPED`, and
  `ingredient_type: SINGLE_INGREDIENT`.
- The active SSSOM surface publishes a `skos:closeMatch` to the active
  anhydrous cerium trichloride parent and a `skos:exactMatch` registry row to
  the local kgmicrobe identifier.
- Exact OLS search for `CeCl3 x 7 H2O` found no active exact ontology term.
  `mappings/hydrate_review.tsv` records this as an established crystalline form
  that needs the local identifier because the anhydrous ChEBI parent and CAS
  `7790-86-5` denote the wrong hydration state.

## Validation

- `uv run --frozen python scripts/validate_strict.py data/ingredients/mapped/Cecl3.yaml data/ingredients/mapped/Cecl3_X_7_H2o.yaml data/ingredients/mapped/Cecropin_A.yaml data/ingredients/mapped/Cecropin_B.yaml data/ingredients/mapped/Cedrelone.yaml`:
  passed.
- `uv run --frozen linkml-term-validator validate-data data/ingredients/mapped/Cecl3.yaml data/ingredients/mapped/Cecl3_X_7_H2o.yaml data/ingredients/mapped/Cecropin_A.yaml data/ingredients/mapped/Cecropin_B.yaml data/ingredients/mapped/Cedrelone.yaml -s src/mediaingredientmech/schema/mediaingredientmech.yaml -t IngredientRecord --labels`:
  passed all 5 external-ontology records in this batch.
- `uv run --frozen python scripts/aggregate_records.py --ingredients-dir data/ingredients --output-dir /tmp/mim_qc_roundtrip.wqYt47`
  followed by
  `uv run --frozen python scripts/verify_roundtrip.py --original-dir data/curated --aggregated-dir /tmp/mim_qc_roundtrip.wqYt47`:
  passed; both curated collection files had 0 data differences and only the
  expected scratch `generation_date` metadata differences.
- `uv run --frozen python scripts/validate_sssom_invariants.py`: passed Rules
  A, B1, B2, B3, C, D, E, F, G, H, I, J, and K. Rule B4 was skipped because
  the sibling kg-microbe ontology transforms were absent.

## Evidence

- Hidden/ignored-inclusive search over `data/curated`, `mappings`,
  `docs/data`, and `reports`, excluding generated review reports and curated
  backups, found both active `MIM:Cecl3_X_7_H2o` SSSOM rows, the
  `mappings/hydrate_review.tsv` row that flagged the inherited anhydrous CAS as
  wrong, the `mappings/other_cross_record_baseline.tsv` hydrate-family row for
  anhydrous `Cecl3`, and matching aggregate/docs rows.
- The current `mappings/culturemech_recipe_membership.tsv` rows contain four
  distinct recipes and four total occurrences for
  `kgmicrobe.compound:cecl3_x_7_h2o`, matching `occurrence_statistics`.
- The inherited `chemical_properties.data_source`, `retrieval_date`, `inchi`,
  and `smiles` still point to PubChem via the anhydrous parent `CHEBI:35458`.
  The InChI and SMILES are byte-identical to anhydrous cerium trichloride and
  do not encode the seven waters of hydration named by this record.
- The current `molecular_formula: CeCl3.7H2O` was generated by appending water
  to the inherited anhydrous formula, and the record does not cite an exact
  external hydrate term or structure source for that formula.
- `TRACE_ELEMENT` has only `COMPUTATIONAL_PREDICTION` evidence from
  `claude_in_session_curation` and is explicitly marked "Provisional
  in-session LLM role assignment; review recommended."

## Completeness

- The local identifier, anhydrous-parent closeMatch, registry identity row, 4/4
  occurrence count, removed anhydrous CAS, SSSOM rows, aggregate copy, and docs
  row are populated.
- Exact hydrate structure fields are incomplete: the retained InChI and SMILES
  still describe anhydrous `CHEBI:35458`, and the retained formula needs a
  cited exact source or should be removed with the other inherited structure
  fields.

## Recommended Edits

- Major: either replace `chemical_properties` with fields from inspected
  evidence for cerium trichloride heptahydrate, or remove the inherited
  anhydrous `data_source`, `retrieval_date`, `molecular_formula`, `inchi`, and
  `smiles`.
- Major: either replace `nutritional_roles.TRACE_ELEMENT` with inspected
  evidence for this hydrate as a trace element in media, or remove the role,
  then rerun strict validation, SSSOM QC, aggregate roundtrip, hydrate audits,
  and `git diff --check`.
